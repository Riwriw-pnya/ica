export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--color-background)]">
      <div className="rounded-xl border border-[var(--color-ink-100)] bg-white p-8 text-center">
        <h1 className="font-heading text-lg font-semibold text-[var(--color-ink-900)]">
          Halaman Login
        </h1>
        <p className="mt-1 text-[12px] text-[var(--color-ink-700)]">
          Form login akan dibuat di sini.
        </p>
      </div>
    </main>
  );
}